# Local dev: use a `.env` file, OR keep the key in `.env.txt` (Vite reads that too).
# Any of these names work:
#   ANTHROPIC_API_KEY=sk-ant-...
#   REACT_APP_ANTHROPIC_API_KEY=sk-ant-...
#
# Vercel: set the same name you use here in Project → Settings → Environment Variables, then redeploy.
